{
  "name": "js13k-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Rainbow Unicorn - Isles of the Sun Door, JS13K 2026 entry",
  "type": "module",
  "scripts": {
    "build": "node build/build.mjs",
    "build:stats": "node build/build.mjs --stats",
    "build:safe": "node build/build.mjs --no-props",
    "build:debug": "node build/build.mjs --no-mangle --no-compress --no-roadroller --keep-comments",
    "dist": "node build/build.mjs && ls -l dist/release.zip",
    "verify": "node build/build.mjs --fast && node verify.mjs",
    "test": "node tests/dungeon.test.mjs && node tests/light.test.mjs && node tests/storm.test.mjs && node tests/runs.test.mjs && node tests/crossing.sim.mjs",
    "check": "npm run test && npm run verify"
  },
  "devDependencies": {
    "roadroller": "^2.1.0",
    "terser": "^5.36.0"
  }
}
